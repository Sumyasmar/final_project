import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com"], // Add the GitHub avatar domain here
  },
  /* other config options here */
};

export default nextConfig;
